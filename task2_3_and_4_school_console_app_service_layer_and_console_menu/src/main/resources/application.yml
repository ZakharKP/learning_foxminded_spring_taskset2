spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/school-db
    username: postgres
    password: 1234
    hikari:
      minimum-idle: 1
      maximum-pool-size: 5
  flyway:
    enabled: true
    locations: classpath:db/migration